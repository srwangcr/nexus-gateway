FROM node:18-alpine
WORKDIR /app
RUN npm init -y && npm install express
EXPOSE 3001
CMD ["node", "-e", "const e = require('express'); const a = e(); a.get('/api/users', (r, s) => s.json({ok:true,users:[]})); a.listen(3001);"]
